# App mode: "mock" for demo/staging (no backend), "live" for production
VITE_APP_MODE=mock

# Backend API (only used when VITE_APP_MODE != mock)
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3002
VITE_RAZORPAY_KEY=
